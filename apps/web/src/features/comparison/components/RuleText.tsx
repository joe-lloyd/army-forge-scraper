// Thin re-export — the canonical implementation lives in components/ui so
// every feature (explorer, analyzer, comparison) renders rules the same way.
export { default, RuleList } from '@/components/ui/RuleText';
